{
  "name": "vedaai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,worker,web -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run worker --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "docker:up": "docker compose up -d",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
